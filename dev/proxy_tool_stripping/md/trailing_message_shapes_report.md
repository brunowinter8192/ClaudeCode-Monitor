# Trailing-message shape probe (total_tokens tag)

## api_requests_opus_wise2627_1788612045

- total stripped texts scanned: 57
- ending-with-tag texts: 56
- distinct shapes: 6

| count | shape (repr, truncated to 200 chars) |
|---|---|
| 39 | `'<total_tokens>N tokens left</total_tokens>'` |
| 12 | `'The following skills are available for use with the Skill tool:\n\n- penny: penny — Skill\n\n<total_tokens>N tokens left</total_tokens>'` |
| 2 | `'The following skills are available for use with the Skill tool:\n\n- penny: Penny purchase planning for four days — pick one product per class by score, check the picks against the daily intake table...` |
| 1 | `'The following deferred tools are now available via ToolSearch. Their schemas are NOT loaded — calling them directly will fail with InputValidationError. Use ToolSearch with query "select:<name>[,<nam...` |
| 1 | `'The following skills are available for use with the Skill tool:\n\n- penny: Penny purchase planning for four days — pick one product per class by score, check the picks against the daily intake table...` |
| 1 | `'Note: /Users/brunowinter2000/Documents/wise2627/.claude/skills/penny/SKILL.md changed on disk since you last read it. That\'s usually deliberate, so take it as the current state rather than reverting...` |

## api_requests_opus_websearch_1788611995

- total stripped texts scanned: 105
- ending-with-tag texts: 87
- distinct shapes: 2

| count | shape (repr, truncated to 200 chars) |
|---|---|
| 86 | `'<total_tokens>N tokens left</total_tokens>'` |
| 1 | `'The following deferred tools are now available via ToolSearch. Their schemas are NOT loaded — calling them directly will fail with InputValidationError. Use ToolSearch with query "select:<name>[,<nam...` |

## api_requests_opus_monitor_cc_1788611156

- total stripped texts scanned: 275
- ending-with-tag texts: 252
- distinct shapes: 19

| count | shape (repr, truncated to 200 chars) |
|---|---|
| 80 | `'<total_tokens>N tokens left</total_tokens>'` |
| 49 | `"First privately list what you need next; then request every item that doesn't depend on another's result in this one response.\n\n<total_tokens>N tokens left</total_tokens>"` |
| 35 | `"First privately list what you need next; then request every item that doesn't depend on another's result in this one response.\n\nOnly you see that command's output — the user's terminal shows at mos...` |
| 35 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\n<total_tokens>N tokens left</total_tokens>"` |
| 16 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nFirst privately list what you need next; then r...` |
| 16 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nOnly you see that command's output — the user's...` |
| 4 | `"First privately list what you need next; then request every item that doesn't depend on another's result in this one response.\n\nPostToolUseFailure:Bash hook additional context: This tool call FAILE...` |
| 4 | `'PostToolUseFailure:Bash hook additional context: This tool call FAILED. 1. Diagnose why before any retry. 2. Retry corrected ONCE. 3. After 2 failures on this goal, STOP and report to the user.\n\n<t...` |
| 2 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nOnly you see that command's output — the user's...` |
| 2 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nOnly you see that command's output — the user's...` |
| 1 | `'The following deferred tools are now available via ToolSearch. Their schemas are NOT loaded — calling them directly will fail with InputValidationError. Use ToolSearch with query "select:<name>[,<nam...` |
| 1 | `"PostToolUseFailure:Bash hook additional context: This tool call FAILED. 1. Diagnose why before any retry. 2. Retry corrected ONCE. 3. After 2 failures on this goal, STOP and report to the user.\n\nFi...` |
| 1 | `"PostToolUseFailure:Bash hook additional context: This tool call FAILED. 1. Diagnose why before any retry. 2. Retry corrected ONCE. 3. After 2 failures on this goal, STOP and report to the user.\n\nOn...` |
| 1 | `"First privately list what you need next; then request every item that doesn't depend on another's result in this one response.\n\nOnly you see that command's output — the user's terminal shows at mos...` |
| 1 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nThe user hasn't heard from you in a while — say...` |
| 1 | `"First privately list what you need next; then request every item that doesn't depend on another's result in this one response.\n\nThe user hasn't heard from you in a while — say in a few words what y...` |
| 1 | `"The user hasn't heard from you in a while — say in a few words what you're doing, then continue.\n\n<total_tokens>N tokens left</total_tokens>"` |
| 1 | `'First privately list what you need next; then request every item that doesn\'t depend on another\'s result in this one response.\n\nOnly you see that command\'s output — the user\'s terminal shows at...` |
| 1 | `'Only you see that command\'s output — the user\'s terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nNote: /Users/brunowinter2000/Documents/ai/mon...` |

## Overall (union across sessions)

- distinct shapes across all 3 sessions: 24

| total count | shape (repr, truncated to 200 chars) |
|---|---|
| 205 | `'<total_tokens>N tokens left</total_tokens>'` |
| 49 | `"First privately list what you need next; then request every item that doesn't depend on another's result in this one response.\n\n<total_tokens>N tokens left</total_tokens>"` |
| 35 | `"First privately list what you need next; then request every item that doesn't depend on another's result in this one response.\n\nOnly you see that command's output — the user's terminal shows at mos...` |
| 35 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\n<total_tokens>N tokens left</total_tokens>"` |
| 16 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nFirst privately list what you need next; then r...` |
| 16 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nOnly you see that command's output — the user's...` |
| 12 | `'The following skills are available for use with the Skill tool:\n\n- penny: penny — Skill\n\n<total_tokens>N tokens left</total_tokens>'` |
| 4 | `"First privately list what you need next; then request every item that doesn't depend on another's result in this one response.\n\nPostToolUseFailure:Bash hook additional context: This tool call FAILE...` |
| 4 | `'PostToolUseFailure:Bash hook additional context: This tool call FAILED. 1. Diagnose why before any retry. 2. Retry corrected ONCE. 3. After 2 failures on this goal, STOP and report to the user.\n\n<t...` |
| 2 | `'The following skills are available for use with the Skill tool:\n\n- penny: Penny purchase planning for four days — pick one product per class by score, check the picks against the daily intake table...` |
| 2 | `'The following deferred tools are now available via ToolSearch. Their schemas are NOT loaded — calling them directly will fail with InputValidationError. Use ToolSearch with query "select:<name>[,<nam...` |
| 2 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nOnly you see that command's output — the user's...` |
| 2 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nOnly you see that command's output — the user's...` |
| 1 | `'The following deferred tools are now available via ToolSearch. Their schemas are NOT loaded — calling them directly will fail with InputValidationError. Use ToolSearch with query "select:<name>[,<nam...` |
| 1 | `'The following skills are available for use with the Skill tool:\n\n- penny: Penny purchase planning for four days — pick one product per class by score, check the picks against the daily intake table...` |
| 1 | `'Note: /Users/brunowinter2000/Documents/wise2627/.claude/skills/penny/SKILL.md changed on disk since you last read it. That\'s usually deliberate, so take it as the current state rather than reverting...` |
| 1 | `"PostToolUseFailure:Bash hook additional context: This tool call FAILED. 1. Diagnose why before any retry. 2. Retry corrected ONCE. 3. After 2 failures on this goal, STOP and report to the user.\n\nFi...` |
| 1 | `"PostToolUseFailure:Bash hook additional context: This tool call FAILED. 1. Diagnose why before any retry. 2. Retry corrected ONCE. 3. After 2 failures on this goal, STOP and report to the user.\n\nOn...` |
| 1 | `"First privately list what you need next; then request every item that doesn't depend on another's result in this one response.\n\nOnly you see that command's output — the user's terminal shows at mos...` |
| 1 | `"Only you see that command's output — the user's terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nThe user hasn't heard from you in a while — say...` |
| 1 | `"First privately list what you need next; then request every item that doesn't depend on another's result in this one response.\n\nThe user hasn't heard from you in a while — say in a few words what y...` |
| 1 | `"The user hasn't heard from you in a while — say in a few words what you're doing, then continue.\n\n<total_tokens>N tokens left</total_tokens>"` |
| 1 | `'First privately list what you need next; then request every item that doesn\'t depend on another\'s result in this one response.\n\nOnly you see that command\'s output — the user\'s terminal shows at...` |
| 1 | `'Only you see that command\'s output — the user\'s terminal shows at most a few lines of it. If the user needs to read any of it, put it in your reply.\n\nNote: /Users/brunowinter2000/Documents/ai/mon...` |